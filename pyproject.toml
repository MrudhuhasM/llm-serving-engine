[project]
name = "llm-serving-engine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "transformers>=5.15.0",
    "uvicorn>=0.52.3",
]
